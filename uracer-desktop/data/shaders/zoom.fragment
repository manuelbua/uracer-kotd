#ifdef GL_ES
precision mediump float;
#endif

varying vec2 v_texCoord0;
uniform sampler2D u_texture;

uniform float offset_x;
uniform float offset_y;
uniform float zoom;

void main()
{
	vec4 c = vec4(0);
	gl_FragColor = texture2D(u_texture, (v_texCoord0 * zoom) + vec2(offset_x, offset_y));
}